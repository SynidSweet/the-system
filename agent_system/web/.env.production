# Production environment configuration for React app
# This will automatically detect the production API URL based on the host
REACT_APP_API_URL=

# Build optimizations
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false

# Production settings
NODE_ENV=production